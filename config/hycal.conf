# run information
Calibration Period = 5
Run Number = 1310

# {key} will be replaced with the value defined to key
# if such a key is not defined, {key} will be empty
Calibration File = database/cal_period{Calibration Period}.dat
Run Info File = database/db_prad_baseinfo_{Run Number}.dat

# components list file paths
Module List = database/hycal_module.txt
DAQ Channel List = database/hycal_daq.txt

# cluster configuration
# now supports "square", "island" or "primex", case insensitive
Cluster Method = primex
Cluster Configuration = config/hycal_cluster.conf
Lead Tungstate Profile = database/prof_pwo.dat
Lead Glass Profile = database/prof_lg.dat

